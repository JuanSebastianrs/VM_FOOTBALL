displayName: test-disk-yaml
jobSpec:
  workerPoolSpecs:
    - machineSpec:
        machineType: n1-standard-4
      replicaCount: 1
      containerSpec:
        imageUri: us-docker.pkg.dev/vertex-ai/training/pytorch-gpu.2-1.py310:latest
        command: ["echo", "hello"]
      diskSpec:
        bootDiskType: pd-standard
        bootDiskSizeGb: 300
